# Quick test configuration for debugging (short runs)

task_id: "BabyAI-GoToObj-v0"
trial: 0

# Training hyperparameters
num_envs: 8
hidden_size: 64
unroll_len: 10
num_updates: 100  # Just 100 updates for quick testing
lr: 1e-3

# Logging
log_interval: 5
wandb_project: "babyai-gru"

# Model architecture
input_dim: 152  # 3*7*7 + 4 + 1
lang_dim: 128

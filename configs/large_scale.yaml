# Large-scale training configuration

task_id: "BabyAI-ActionObjDoor-v0"
trial: 0

# Training hyperparameters
num_envs: 32  # More parallel envs
hidden_size: 256  # Larger model
unroll_len: 40  # Longer unrolls
num_updates: 10000  # More updates
lr: 5e-4  # Lower learning rate for stability

# Logging
log_interval: 20
wandb_project: "babyai-gru"

# Model architecture
input_dim: 152  # 3*7*7 + 4 + 1
lang_dim: 128
